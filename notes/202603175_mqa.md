# 202603175_mqa

**Hypothesis**: Multi-Query Attention (MQA) uses 1 KV head shared across all Q heads. With dim=256, HEAD_DIM=128 (2 Q heads), MQA halves K/V projection params. 2026 MASA paper shows near-zero quality cost at this scale. Freed params can be reallocated naturally via wider MLP.
**Target Metric**: val_bpb < 1.393200
**Code Change**: build_model_config sets n_kv_head=1 instead of n_kv_head=num_heads.

---
**Result**: val_bpb=1.402906
**Git Hash**: 6e2f68b
**Verdict**: DISCARD. Worse than full MHA (1.393200). With only 2 Q heads total, sharing 1 KV head removes too much representational capacity.
