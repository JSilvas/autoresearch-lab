# 2026031791_gelu

**Hypothesis**: MLP activation is ReLU² (F.relu(x).square()), confirmed much better than plain ReLU (Exp44: +0.027). GELU is a smooth activation that differs from both — it's a Gaussian error linear unit that approximates a soft version of ReLU with non-zero gradients for negative inputs. For small models and language tasks, GELU is commonly preferred over ReLU variants. Try F.gelu(x) as drop-in replacement (same FLOPs, same parameter count, just different nonlinearity).
**Target Metric**: val_bpb < 1.369316
**Code Change**: MLP forward: F.relu(x).square() → F.gelu(x). WD sqrt decay reverted to linear.

---
**Result**: val_bpb=1.375996
**Git Hash**: ea74421
**Verdict**: DISCARD. Worse (+0.006680). Activation ranking: ReLU² (best) >> GELU (+0.007) >> ReLU (+0.027). ReLU² sparsity with quadratic scale is optimal for this architecture.
