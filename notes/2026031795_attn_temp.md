# 2026031795_attn_temp

**Hypothesis**: After QK-norm, Q and K vectors each have L2 norm ≈ sqrt(head_dim)=sqrt(128)≈11.3. So Q·K ≈ 128*cos(θ), and with default scale=1/sqrt(128), attention logits = sqrt(128)*cos(θ) ≈ 11.3*cos(θ). Doubling the scale to 2/sqrt(128) gives logits ≈ 22.6*cos(θ), making attention sharper and more selective. With S=64 sliding window (64 tokens), sharper attention should help the model focus on the most relevant nearby tokens rather than broadly attending. The default 1/sqrt(d) is calibrated for unnormalized Q/K; with QK-norm, the optimal temperature is unknown and worth tuning.
**Target Metric**: val_bpb < 1.369316
**Code Change**: F.scaled_dot_product_attention: add scale=2.0/(self.head_dim**0.5) argument. Z_LOSS_COEFF removed (z-loss reverted).

---
**Result**: TBD
**Git Hash**: TBD
**Verdict**: TBD
