# 2026031745_ve_lastonly

**Hypothesis**: Current VE on layers 0 and 2 (2/3 layers). Last layer (2) is confirmed most critical (SSL window, full attention last). Remove VE from layer 0, keep only last layer. Saves ~37K params → more optimizer steps. The VE on the first layer may be redundant since the residual stream hasn't been built up yet.
**Target Metric**: val_bpb < 1.375674
**Code Change**: has_ve() → return layer_idx == n_layer - 1 (only last layer)

---
**Result**: val_bpb=1.382688
**Git Hash**: 18e1901
**Verdict**: DISCARD. Layer 0 VE matters (+0.007 without it). VE trend: 0 VE=1.432, 1 VE=1.383, 2 VE=1.376. Try all 3 layers.
