# 2026031797_half_batch

**Hypothesis**: TOTAL_BATCH_SIZE=32768 (current) gives ~416 optimizer steps in 300s at 45k tok/sec. Halving to 16384 gives ~832 steps — 2× more weight updates per time budget. With grad_accum_steps dropping from 2 to 1, each step processes 16384 tokens in a single forward pass (no accumulation overhead). The model has been step-starved: DEPTH=3 beats DEPTH=4+ precisely because it gives more steps. If the step count limit is the binding constraint, halving batch size could help significantly. The cost is noisier gradients per step, but Muon's orthogonalization normalizes matrix gradient norms implicitly. Test at same LRs first to see if more steps outweigh higher noise.
**Target Metric**: val_bpb < 1.369316
**Code Change**: TOTAL_BATCH_SIZE: 2**15 (32768) → 2**14 (16384). ATTN_TEMP_SCALE reverted to 1.0 (attention temp experiment reverted).

---
**Result**: val_bpb=1.374779
**Git Hash**: (fix commit)
**Verdict**: DISCARD. Worse (+0.005463). More steps (875 vs 416) at half batch is competitive but not better. The noisier gradients at MATRIX_LR=0.080 slightly hurt. Would need LR retuning for half-batch (linear scaling rule: MATRIX_LR~0.040). Not worth pursuing further when best is already at full-batch config.
