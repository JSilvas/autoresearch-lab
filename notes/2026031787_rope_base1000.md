# 2026031787_rope_base1000

**Hypothesis**: RoPE base frequency is hardcoded at 10000. Lower base = positions rotate faster = model implicitly focuses attention on shorter-range interactions. With SSL window pattern (S=64 local windows in layers 0,1), the dominant attention patterns are already local. Lowering RoPE base to 1000 aligns the positional encoding bias with the short-range structure the model has learned to exploit. Higher base (10000) is designed for long sequences; at seq_len=2048 with effective S=64, 1000 may be better calibrated.
**Target Metric**: val_bpb < 1.369316
**Code Change**: RoPE `_precompute_rotary_embeddings` base param: 10000 → 1000 via ROPE_BASE hyperparameter

---
**Result**: val_bpb=1.382689
**Git Hash**: af41296
**Verdict**: DISCARD. Catastrophic (+0.013373). RoPE base=1000 destroys positional encoding — at seq_len=2048 positions wrap multiple times, aliasing near/far positions. 10000 is well-calibrated for 2048 sequences.
