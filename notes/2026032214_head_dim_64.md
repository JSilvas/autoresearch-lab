# Hypothesis
Reducing the head dimension from 128 to 64 (increasing the number of heads from 2 to 3) will improve the model's ability to capture diverse features, leading to lower val_bpb.

# Target Metric
val_bpb (lower is better)

# Code Change
Change HEAD_DIM from 128 to 64 in train.py.