# 202603173_swiglu

**Hypothesis**: SwiGLU (Swish-Gated Linear Unit) consistently beats ReLU² in 2025-2026 literature. Requires 3-projection MLP (up, gate, down). Iso-parameter: hidden=682 instead of 1024 to keep same param count.
**Target Metric**: val_bpb < 1.393200
**Code Change**: Replace ReLU² MLP (c_fc→ReLU²→c_proj) with SwiGLU (c_gate + c_fc→SiLU→c_gate_prod→c_proj). Hidden dim 682 = floor(4 * 256 * 2/3).

---
**Result**: val_bpb=1.399548
**Git Hash**: 6edf0ba
**Verdict**: DISCARD. Worse than ReLU² (1.393200). SwiGLU benefits don't materialize at this tiny scale / short training budget. ReLU² stays.
