# 2026031782_wd_constant

**Hypothesis**: Current WD decays linearly from 0.15 to 0 over training (`WD*(1-progress)`). This means near-zero regularization in the warmdown phase. With WARMDOWN_RATIO=0.60 (60% of training in decay), the model gets very little regularization for most of training. Try constant WD=0.15 throughout — provides consistent regularization even during the long warmdown phase, potentially preventing overfitting to frequent patterns.
**Target Metric**: val_bpb < 1.369316
**Code Change**: `get_weight_decay`: `WD*(1-progress)` → `WD` (constant)

---
**Result**: TBD
**Git Hash**: TBD
**Verdict**: TBD
