# 2026031744_relu_plain

**Hypothesis**: Current activation is ReLU² (relu(x).square()). This promotes sparse, high-magnitude activations. Plain ReLU has less sparsity, more gradient flow through near-zero activations. With DEPTH=3 and only 192-dim, denser activations might allow better information flow through the shallow network.
**Target Metric**: val_bpb < 1.375674
**Code Change**: F.relu(x).square() → F.relu(x)

---
**Result**: TBD
**Git Hash**: TBD
**Verdict**: TBD
